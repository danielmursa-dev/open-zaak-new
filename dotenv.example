SECRET_KEY="django-insecure-%@6wccg*r1(1is#i)m3q34pa25_&0n-h(2tp#q(r9*0z4%jk5x"
DB_NAME="openzaak_new"
DB_USER="openzaak_new"
#
# For peer authentication via unix domain socket
# these should be kept empty.
#
DB_PASSWORD=""
DB_HOST=""
DB_PORT=""

# Uncomment to record HTTP fixtures for the VCR tests
# Should be fine to leave as "once" in dev, but *not* in CI.
# VCR_RECORD_MODE="once"
